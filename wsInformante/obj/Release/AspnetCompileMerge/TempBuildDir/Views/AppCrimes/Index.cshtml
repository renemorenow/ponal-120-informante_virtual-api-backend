@model IEnumerable<wsInformante.Models.APP_VI_CRIMES>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPTION)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LEGAL_INFORMATION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPTION)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LEGAL_INFORMATION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CRIME_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CRIME_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CRIME_ID })
        </td>
    </tr>
}

</table>
